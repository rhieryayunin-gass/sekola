import { Body,Controller,Delete,Get,Param,Patch,Post,Req,UseGuards } from "@nestjs/common";import { Request } from "express";import { RequirePermission } from "../../common/authorization/decorators/require-permission.decorator";import { PermissionGuard } from "../../common/authorization/guards/permission.guard";import { AuthGuard } from "../auth/guards/auth.guard";import { AssessmentService } from "./assessment.service";type Resource=Parameters<AssessmentService["list"]>[1];@UseGuards(AuthGuard,PermissionGuard)class B{constructor(public s:AssessmentService){}u(r:Request){if(!r.user)throw new Error("Authenticated user missing");return r.user.id}}function resourceController(path:string,resource:Resource,permission:string){@Controller(path)class C extends B{@Get()@RequirePermission(`${permission}.read`)l(@Req()r:Request){return this.s.list(this.u(r),resource)}@Post()@RequirePermission(`${permission}.create`)c(@Req()r:Request,@Body()b:Record<string,unknown>){return this.s.create(this.u(r),resource,b)}@Patch(":id")@RequirePermission(`${permission}.update`)patch(@Req()r:Request,@Param("id")id:string,@Body()b:Record<string,unknown>){return this.s.update(this.u(r),resource,id,b)}@Delete(":id")@RequirePermission(`${permission}.delete`)d(@Req()r:Request,@Param("id")id:string){return this.s.remove(this.u(r),resource,id)}}return C}export const AttendanceController=resourceController("attendance","attendance_records","attendance");export const AttendanceQrController=resourceController("attendance/qr-sessions","attendance_qr_sessions","attendance_qr");export const ExamsController=resourceController("exams","exams","exams");export const ExamQuestionsController=resourceController("exam-questions","exam_questions","exam_questions");export const ExamSessionsController=resourceController("exam-sessions","exam_sessions","exam_sessions");export const ExamResultsController=resourceController("exam-results","exam_results","exam_results");@Controller("exam-analytics")@UseGuards(AuthGuard,PermissionGuard)export class ExamAnalyticsController extends B{@Get()@RequirePermission("exam_analytics.read")l(@Req()r:Request){return this.s.analytics(this.u(r))}}
