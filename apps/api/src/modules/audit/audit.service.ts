import { Injectable } from "@nestjs/common"; import { SupabaseService } from "../../common/supabase/supabase.service";
@Injectable() export class AuditService { constructor(private readonly supabase:SupabaseService){} async record(input:{tenantId:string;actorUserId?:string;action:string;module:string;resourceType:string;resourceId?:string;beforeState?:unknown;afterState?:unknown}){const {error}=await this.supabase.getClient().from("audit_logs").insert({tenant_id:input.tenantId,actor_user_id:input.actorUserId??null,action:input.action,module:input.module,resource_type:input.resourceType,resource_id:input.resourceId??null,before_state:input.beforeState??null,after_state:input.afterState??null});if(error)throw error;}}
