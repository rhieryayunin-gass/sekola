import { readFileSync } from "node:fs";import { resolve } from "node:path";import { describe,expect,it } from "vitest";describe("Learning migrations",()=>it("keeps phases 23-26 tenant scoped",()=>{for(const [f,t]of[["0028_courses.sql","courses"],["0029_lessons.sql","lessons"],["0030_assignments.sql","assignments"],["0031_submissions.sql","submissions"]]){const x=readFileSync(resolve(process.cwd(),`database/migrations/${f}`),"utf8");expect(x).toContain(`public.${t}`);expect(x).toContain("tenant_id uuid not null");expect(x).toContain(`alter table public.${t} enable row level security`)}}));
