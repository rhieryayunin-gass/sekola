"use client";
import { useMutation,useQuery,useQueryClient } from "@tanstack/react-query";
import { useMemo,useState,type FormEvent } from "react";
import { Plus,ChevronLeft,ChevronRight,CalendarDays,Clock } from "lucide-react";
import { browserApi } from "../../lib/api/browser";
import { schoolRpc } from "../../lib/school";
import { usePermissionStore } from "../../stores/permission-store";
import { occurrences,dateKey,localInput,type SchoolEvent,type Occurrence } from "../../lib/calendar-occurrences";
import { Button,Input,Select } from "../ui";
import { Modal } from "../ui/modal";
type Calendar={id:string;name:string;can_edit:boolean;integration_managed:boolean};
type Context={calendars:Calendar[];events:SchoolEvent[]};
type View="month"|"week"|"day"|"agenda";
const labels:Record<View,string>={month:"Bulan",week:"Minggu",day:"Hari",agenda:"Agenda"};
export function CalendarManager(){
 const qc=useQueryClient();const userId=usePermissionStore(s=>s.context?.userId);const has=usePermissionStore(s=>s.has);
 const [date,setDate]=useState(()=>new Date());const [view,setView]=useState<View>("month");const [hidden,setHidden]=useState<string[]>([]);const [detail,setDetail]=useState<Occurrence>();const [editor,setEditor]=useState<SchoolEvent|"new"|"calendar">();const [error,setError]=useState("");
 const range=useMemo(()=>{const start=new Date(date.getFullYear(),date.getMonth(),view==="month"||view==="agenda"?1:date.getDate());if(view==="month"||view==="week")start.setDate(start.getDate()-(start.getDay()+6)%7);const end=new Date(start);end.setDate(end.getDate()+(view==="month"?42:view==="week"?7:view==="day"?1:31));return {start,end};},[date,view]);
 const q=useQuery({queryKey:["school-calendar",userId,range.start.toISOString(),range.end.toISOString()],enabled:!!userId,queryFn:()=>schoolRpc<Context>("school_calendar_context",{starts:range.start.toISOString(),ends:range.end.toISOString()})});
 const events=useMemo(()=>occurrences((q.data?.events??[]).filter(e=>!hidden.includes(e.calendar_id)),range.start,range.end),[q.data,hidden,range]);
 const days=Array.from({length:view==="month"?42:view==="week"?7:1},(_,i)=>{const d=new Date(range.start);d.setDate(d.getDate()+i);return d;});
 const dayEvents=(d:Date)=>{const end=new Date(d);end.setDate(end.getDate()+1);return events.filter(e=>e.start<end&&e.end>=d);};
 const color=(id:string)=>`var(--calendar-${Math.max(0,q.data?.calendars.findIndex(c=>c.id===id)??0)%5})`;
 const save=useMutation({mutationFn:({path,body,method="POST"}:{path:string;body?:object;method?:string})=>browserApi(path,{method,...(body?{body:JSON.stringify(body)}:{})}),onSuccess:async()=>{setEditor(undefined);setDetail(undefined);await qc.invalidateQueries({queryKey:["school-calendar"]});},onError:e=>setError(e.message)});
 function shift(direction:number){const d=new Date(date);if(view==="month"||view==="agenda"){d.setDate(1);d.setMonth(d.getMonth()+direction);}else d.setDate(d.getDate()+direction*(view==="week"?7:1));setDate(d);}
 function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError("");const f=new FormData(e.currentTarget);if(editor==="calendar"){save.mutate({path:"/calendars",body:{name:f.get("name")}});return;}const start=new Date(String(f.get("starts_at")));const end=new Date(String(f.get("ends_at")));if(end<=start){setError("Waktu selesai harus setelah waktu mulai.");return;}const repeat=String(f.get("repeat")??"");const existing=typeof editor==="object"?editor:undefined;save.mutate({path:`/calendars/${existing?.calendar_id??f.get("calendar_id")}/events${existing?`/${existing.id}`:""}`,method:existing?"PATCH":"POST",body:{title:f.get("title"),description:f.get("description"),starts_at:start.toISOString(),ends_at:end.toISOString(),is_all_day:f.get("is_all_day")==="on",event_type:f.get("event_type"),recurrence_rule:repeat==="KEEP"?existing?.recurrence_rule:repeat?`FREQ=${repeat};COUNT=${Number(f.get("count"))||12}`:null}});}
 function eventButton(event:Occurrence){return <button key={event.instance} className="school-calendar-event" style={{borderLeftColor:color(event.calendar_id)}} onClick={()=>{setDetail(event);setError("");}}><span>{event.is_all_day?"":event.start.toLocaleTimeString("id-ID",{hour:"2-digit",minute:"2-digit"})+" "}</span>{event.title}</button>;}
 return <section className="school-calendar"><header className="school-calendar-toolbar"><CalendarDays size={25}/><h2>{date.toLocaleDateString("id-ID",{month:"long",year:"numeric"})}</h2><Button variant="ghost" aria-label="Periode sebelumnya" onClick={()=>shift(-1)}><ChevronLeft size={18}/></Button><Button variant="ghost" onClick={()=>setDate(new Date())}>Hari ini</Button><Button variant="ghost" aria-label="Periode berikutnya" onClick={()=>shift(1)}><ChevronRight size={18}/></Button><Select aria-label="Tampilan kalender" value={view} onChange={e=>setView(e.target.value as View)}>{Object.entries(labels).map(([key,label])=><option key={key} value={key}>{label}</option>)}</Select>{has("calendar.create")&&<Button onClick={()=>{setEditor("new");setError("");}} disabled={!q.data?.calendars.some(c=>c.can_edit)}><Plus size={16}/>Acara</Button>}</header><div className="school-calendar-layout"><aside className="school-calendar-sidebar"><h3>Kalender saya</h3>{q.data?.calendars.map(c=><label key={c.id}><input type="checkbox" checked={!hidden.includes(c.id)} onChange={e=>setHidden(old=>e.target.checked?old.filter(id=>id!==c.id):[...old,c.id])}/><span style={{background:color(c.id)}}/>{c.name}</label>)}{has("calendar.create")&&<Button variant="ghost" onClick={()=>{setEditor("calendar");setError("");}}>+ Kalender baru</Button>}<small>Waktu mengikuti zona perangkat Anda.</small></aside><div className="school-calendar-main">{q.isError?<p role="alert" className="school-error">{q.error.message}<Button onClick={()=>void q.refetch()}>Coba lagi</Button></p>:q.isLoading?<p role="status">Memuat agenda…</p>:view==="month"?<><div className="school-week-labels">{["Sen","Sel","Rab","Kam","Jum","Sab","Min"].map(d=><span key={d}>{d}</span>)}</div><div className="school-month-grid">{days.map(day=><div className="school-month-day" key={dateKey(day)} data-outside={day.getMonth()!==date.getMonth()} data-today={dateKey(day)===dateKey(new Date())}><button className="school-day-number" onClick={()=>{setDate(day);setView("day");}}>{day.getDate()}</button>{dayEvents(day).slice(0,3).map(eventButton)}{dayEvents(day).length>3&&<button onClick={()=>{setDate(day);setView("day");}}>+{dayEvents(day).length-3} lainnya</button>}</div>)}</div></>:view==="agenda"?<div className="school-agenda">{events.length?events.map(event=><article key={event.instance}><time>{event.start.toLocaleDateString("id-ID",{weekday:"short",day:"numeric",month:"short"})}</time>{eventButton(event)}</article>):<p className="school-empty">Tidak ada acara pada periode ini.</p>}</div>:<div className="school-time-scroll"><div className="school-time-grid" style={{gridTemplateColumns:`52px repeat(${days.length},minmax(120px,1fr))`}}><div className="school-time-hours"><span>Waktu</span>{Array.from({length:24},(_,h)=><small key={h}>{String(h).padStart(2,"0")}:00</small>)}</div>{days.map(day=><div key={dateKey(day)} className="school-time-day"><header><strong>{day.toLocaleDateString("id-ID",{weekday:"short",day:"numeric"})}</strong>{dayEvents(day).filter(e=>e.is_all_day).map(eventButton)}</header><div className="school-time-track">{dayEvents(day).filter(e=>!e.is_all_day).map(e=>{const minutes=Math.max(0,(e.start.getTime()-day.getTime())/60000);const endMinutes=Math.min(1440,(e.end.getTime()-day.getTime())/60000);return <div key={e.instance} className="school-time-event" style={{top:minutes/60*52,height:Math.max(30,(endMinutes-minutes)/60*52)}}>{eventButton(e)}</div>;})}</div></div>)}</div></div>}</div></div>
 <Modal isOpen={!!detail} onClose={()=>setDetail(undefined)} title={detail?.title??"Acara"}>{detail&&<div className="grid gap-4"><p><Clock size={16}/>{detail.start.toLocaleString("id-ID")} — {detail.end.toLocaleString("id-ID")}</p><p>{detail.description}</p><p>{q.data?.calendars.find(c=>c.id===detail.calendar_id)?.name}</p>{detail.recurrence_rule&&<p>Acara berulang. Perubahan berlaku untuk seluruh rangkaian.</p>}{detail.recurrence_error&&<p role="alert">Pola perulangan belum dapat ditampilkan.</p>}{q.data?.calendars.find(c=>c.id===detail.calendar_id)?.can_edit&&<div className="flex gap-3">{has("calendar.update")&&<Button onClick={()=>{const source=q.data.events.find(e=>e.id===detail.id);setEditor(source);setDetail(undefined);}}>Edit acara</Button>}{has("calendar.delete")&&<Button variant="ghost" disabled={save.isPending} onClick={()=>{if(confirm("Hapus acara ini beserta seluruh perulangannya?"))save.mutate({path:`/calendars/${detail.calendar_id}/events/${detail.id}`,method:"DELETE"});}}>Hapus</Button>}</div>}{error&&<p role="alert">{error}</p>}</div>}</Modal>
 <Modal isOpen={!!editor} onClose={()=>setEditor(undefined)} title={editor==="calendar"?"Kalender baru":typeof editor==="object"?"Edit acara":"Acara baru"}><form key={typeof editor==="object"?editor.id:editor} className="grid gap-4" onSubmit={submit}>{editor==="calendar"?<Input name="name" label="Nama kalender" required maxLength={160}/>:<><Input name="title" label="Judul acara" defaultValue={typeof editor==="object"?editor.title:""} required maxLength={200}/><Select name="calendar_id" label="Kalender" disabled={typeof editor==="object"} defaultValue={typeof editor==="object"?editor.calendar_id:q.data?.calendars.find(c=>c.can_edit)?.id} required>{q.data?.calendars.filter(c=>c.can_edit).map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</Select><Input name="starts_at" label="Mulai" type="datetime-local" defaultValue={typeof editor==="object"?localInput(editor.starts_at):dateKey(date)+"T08:00"} required/><Input name="ends_at" label="Selesai" type="datetime-local" defaultValue={typeof editor==="object"?localInput(editor.ends_at??editor.starts_at):dateKey(date)+"T09:00"} required/><label><input type="checkbox" name="is_all_day" defaultChecked={typeof editor==="object"&&editor.is_all_day}/> Sepanjang hari</label><Select name="event_type" label="Kategori" defaultValue={typeof editor==="object"?editor.event_type:"GENERAL"}>{["GENERAL","MEETING","HOLIDAY","DEADLINE"].map(t=><option key={t}>{t}</option>)}</Select><Select name="repeat" label="Perulangan" defaultValue={typeof editor==="object"&&editor.recurrence_rule?"KEEP":""}><option value="">Tidak berulang</option>{typeof editor==="object"&&editor.recurrence_rule&&<option value="KEEP">Pertahankan pola sekarang</option>}<option value="DAILY">Harian</option><option value="WEEKLY">Mingguan</option><option value="MONTHLY">Bulanan</option></Select><Input type="number" name="count" label="Jumlah kejadian jika berulang" min={1} max={366} defaultValue={12}/><Input name="description" label="Keterangan" defaultValue={typeof editor==="object"?editor.description??"":""}/></>}{error&&<p role="alert" className="school-error">{error}</p>}<Button type="submit" disabled={save.isPending}>Simpan</Button></form></Modal>
 </section>;
}
