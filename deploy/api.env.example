# Copy to deploy/api.env.local and restrict to mode 0600 on the deployment host.
# Never put this file or the service key into build args, source control or web env.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
CORS_ORIGINS=
# RELEASE_SHA, NODE_ENV and PORT come from the immutable application image.
