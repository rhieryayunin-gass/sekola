# Copy to deploy/production.env.local (public build/deployment configuration).
# Build frontend images again whenever these public values change.
RELEASE_SHA=
WEB_DOMAIN=
API_DOMAIN=
ACME_EMAIL=
NEXT_PUBLIC_SUPABASE_URL=
# Publishable key preferred; legacy anon JWT also supported in this variable.
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
