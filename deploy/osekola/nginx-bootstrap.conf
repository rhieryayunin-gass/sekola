# Temporary HTTP vhost used only to obtain api.osekola.com's certificate.
server {
    listen 80;
    server_name api.osekola.com;
    access_log off;
    location /.well-known/acme-challenge/ { root /var/www/osekola-acme; }
    location / { return 503; }
}
